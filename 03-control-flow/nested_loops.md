# nested_loops.c

nested_loops.c
Demonstrates nested loops and pattern printing

Topics: Nested loops, pattern printing, multiplication tables

## Overview

- **File:** `03-control-flow/nested_loops.c`
- **Language:** C
- **Module:** `03-control-flow`

## Key Concepts

This file demonstrates essential C programming concepts including:
- Standard input/output operations using `printf` and `scanf`

## Functions

- `printf()`
- `loop()`
- `table()`
- `triangle()`

## Compilation

```bash
# Navigate to the module directory
cd 03-control-flow

# Compile
gcc nested_loops.c -o nested_loops -std=c11 -Wall -Wextra

# Run
./nested_loops
```

## Usage

Run the compiled program to see the demonstrated concepts in action.
