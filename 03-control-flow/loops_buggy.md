# loops_buggy.c

loops_buggy.c
DELIBERATELY BUGGY CODE - For learning purposes

This file contains common mistakes with loops.
Each bug is explained with comments.

## Overview

- **File:** `03-control-flow/loops_buggy.c`
- **Language:** C
- **Module:** `03-control-flow`

## Key Concepts

This file demonstrates essential C programming concepts including:
- Standard input/output operations using `printf` and `scanf`

## Functions

- `error()`
- `printf()`
- `value()`
- `num()`

## Compilation

```bash
# Navigate to the module directory
cd 03-control-flow

# Compile
gcc loops_buggy.c -o loops_buggy -std=c11 -Wall -Wextra

# Run
./loops_buggy
```

## Usage

Run the compiled program to see the demonstrated concepts in action.
